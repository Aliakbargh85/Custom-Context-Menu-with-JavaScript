# Custom-Context-Menu-with-JavaScript
A simple custom right-click context menu built with HTML, CSS, and vanilla JavaScript. It replaces the browser’s default context menu and dynamically positions itself based on the cursor
